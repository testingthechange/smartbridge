name: Render Deploy Ping

on:
  push:
    branches:
      - main

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm push event
        run: echo "GitHub push detected on main branch"
